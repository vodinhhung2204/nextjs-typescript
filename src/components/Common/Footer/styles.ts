import { makeStyles } from '@mui/styles'
export const useStyles = makeStyles((theme) => ({
	containerFooter: {
		height: '44px',
		width: '100%',
		// position: 'fixed',
		// bottom: '0',
		backgroundColor: 'red',
	},
}))
