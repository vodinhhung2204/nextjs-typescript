export const initialState = {
	data: 'Data homepage in reducer',
}

function homeReducer(state = initialState, action: any) {
	switch (action.type) {
		default:
			return state
	}
}

export default homeReducer
